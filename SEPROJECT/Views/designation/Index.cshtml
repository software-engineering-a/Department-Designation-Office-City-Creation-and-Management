@model IEnumerable<SEPROJECT.Models.designation>

@{
    ViewBag.Title = "Index";
}
<style>
    #propertycreate {
        border: 1px solid black;
        padding: 25px 50px 75px;
    }
</style>
<div id="propertycreate" style=background-color:silver>
    <h2>Available Designations</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.des_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.status)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.des_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.status)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.des_id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.des_id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.des_id })
                </td>
            </tr>
        }

    </table>
    </div>