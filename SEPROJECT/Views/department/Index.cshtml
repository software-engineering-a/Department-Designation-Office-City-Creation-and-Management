@model IEnumerable<SEPROJECT.Models.department>

@{
    ViewBag.Title = "Index";
}
<style>
    #propertycreate {
        border: 1px solid black;
        padding: 25px 50px 75px;
    }
</style>
<div id="propertycreate" style=background-color:silver>
    <h2>Available Departments</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.dep_name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.dep_manager)
            </th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.dep_name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.dep_manager)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.dep_id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.dep_id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.dep_id })
                </td>
            </tr>
        }

    </table>
    </div>
